import React from 'react';

/**
 * LiveSalesCounter — DÉSACTIVÉ POUR LE CONCOURS 2026.
 *
 * L'ancienne implémentation générait des notifications de "ventes en direct"
 * à partir de prénoms, villes et produits fictifs (fake social proof). Cela
 * ne respecte pas la règle du concours : rien ne doit être faux ou non prouvé.
 *
 * Le composant renvoie null pour ne rien afficher. Il pourra être réactivé
 * plus tard en lisant les vraies commandes récentes depuis l'API (avec
 * consentement explicite des clientes concernées).
 */
const LiveSalesCounter = () => null;

export default LiveSalesCounter;
